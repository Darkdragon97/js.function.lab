console.log("It's working")
const maxOfTwoNumbers =(x, y){
  if (x >= y) => {
    return x;
  } else {
    return y;
  }
}
console.log('Exercise 1 Result:', maxOfTwoNumbers(3, 9));

const isAdult = (isAdult 21, Adult) {
  if (isAdult(21) >= Adult) => {
    return Adult;
    else {
      return 21;
    }
  }
}

